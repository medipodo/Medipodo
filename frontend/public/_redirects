# Netlify/Vercel Redirects and Rewrites

# Sitemap and robots.txt
/sitemap.xml /sitemap.xml 200
/robots.txt /robots.txt 200

# WebP image serving (optional - for future optimization)
# If browser supports WebP, serve WebP version
# /images/gallery/*.jpg /images/gallery/:splat.webp 200

# SPA fallback - must be last
/* /index.html 200
